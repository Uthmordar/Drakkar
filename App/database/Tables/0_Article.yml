table: articles
columns:
    id: 
        INT(11): unsigned|auto_increment
    name: 
        VARCHAR(255): not null
    password: 
        VARCHAR(32): not null
    email: 
        TEXT: not null
    status:
        ENUM('visitor', 'editor', 'admin'): default 'visitor'|not null
keys:
    primary: id